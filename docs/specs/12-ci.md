# Spec 12 — CI

**Status:** complete

## Goal

Set up GitHub Actions CI to run typecheck, lint, test, and build on every push and PR.

## Dependencies

- Spec 00 (project setup) complete — pnpm, TypeScript, vitest, eslint configured

## Deliverables

| File | Description |
|------|-------------|
| `.github/workflows/ci.yml` | GitHub Actions CI workflow |

## Key interfaces / signatures

N/A — this is an infrastructure spec.

## Implementation notes

### Workflow configuration

```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm build
```

### Key decisions
- Test on Node 20 (LTS) and 22 (current).
- Use `pnpm/action-setup@v4` for pnpm installation.
- Use `--frozen-lockfile` to ensure CI uses the exact lockfile.
- Run all four checks: typecheck, lint, test, build.
- Single job with matrix — no need for separate jobs at this project size.

## Acceptance criteria

- Workflow file exists at `.github/workflows/ci.yml`
- Workflow runs successfully on push to main
- All four steps (typecheck, lint, test, build) execute
- Matrix covers Node 20 and 22

## Do not do

- Do not add deployment steps — that is spec 13.
- Do not add Snyk or security scanning in this spec.
- Do not add coverage reporting to external services.
- Do not add release automation.
